export function buildWaterfall(trace:{start_time:string;end_time:string},spans:any[]){const start=Date.parse(trace.start_time),duration=Math.max(1,Date.parse(trace.end_time)-start),byParent=new Map<string|null,any[]>();for(const span of spans){const key=span.parent_span_id||null;byParent.set(key,[...(byParent.get(key)||[]),span])}const result:any[]=[];const visit=(parent:string|null,depth:number)=>{for(const span of (byParent.get(parent)||[]).sort((a,b)=>Date.parse(a.start_time)-Date.parse(b.start_time)||a.span_id.localeCompare(b.span_id))){const left=Math.max(0,(Date.parse(span.start_time)-start)/duration*100),width=Math.max(1,(Date.parse(span.end_time)-Date.parse(span.start_time))/duration*100);result.push({...span,depth,left,width});visit(span.span_id,depth+1)}};visit(null,0);return result}
