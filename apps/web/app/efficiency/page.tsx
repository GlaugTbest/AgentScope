import { AppHeader } from '../../components/app-header';
export default function EfficiencyPage(){return <main className="app-shell"><AppHeader/><section className="observatory standalone"><span className="overline">Eficiência baseada em evidências</span><h1>Experimentos e qualidade</h1><p>Use a API local para registrar avaliações, catálogo de preços simulados e comparações baseline/variante. Cada recomendação expõe as amostras, a variação de qualidade, tokens e latência que a sustentam.</p><div className="empty-traces"><h2>Endpoints prontos para integração</h2><p><code>POST /v1/evaluations</code>, <code>POST /v1/prices</code> e <code>POST /v1/experiments</code> preservam os dados e retornam a recomendação determinística.</p></div></section></main>}
